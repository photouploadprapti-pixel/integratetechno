import { SisReportsPanel } from '@/components/admin/sis-reports-panel'

/**
 * Super Admin S/I/S Report module with live Supabase data.
 */
export default function SisReportPage() {
  return <SisReportsPanel />
}
