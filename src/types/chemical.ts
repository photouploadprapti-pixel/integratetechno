/** Brand partner shown on the Chemical Division page. */
export interface ChemicalBrand {
  id: string
  name: string
  logo: string
  description: string
  highlight: string
}

/** Hero slide for the Chemical Division carousel. */
export interface ChemicalHeroSlide {
  id: string | number
  image: string
}
